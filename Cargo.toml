[workspace]
resolver = "2"
members = ["crates/demodesk-core", "src-tauri"]
exclude = ["vendor"]

[workspace.package]
edition = "2021"
version = "1.0.20"
license = "AGPL-3.0-only"

# `tauri dev` builds the dev profile; the demo parser is ~10× slower unoptimized,
# so optimize dependencies (vendored parser included) even there.
[profile.dev.package."*"]
opt-level = 2

# Native whole-match animation and scoring also run inside the workspace core.
[profile.dev.package.demodesk-core]
opt-level = 3

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = true
